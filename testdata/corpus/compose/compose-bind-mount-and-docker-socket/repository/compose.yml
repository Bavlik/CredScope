services:
  agent:
    image: example.invalid/corpus/agent:1.0
    environment:
      AGENT_TOKEN: ${AGENT_TOKEN}
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./data:/app/data
