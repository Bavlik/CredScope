This repository fixture is intentionally empty of GitHub Actions workflows and
Docker Compose files. It exists only so the case has a valid repository root
to scan; the only input under test is the imported Gitleaks report.
